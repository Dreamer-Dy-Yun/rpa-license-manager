{
  "name": "rpa-license-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/domain",
    "functions"
  ],
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "domain:build": "npm --workspace @rpa-license/domain run build",
    "typecheck": "npm run domain:build && tsc --noEmit",
    "test": "npm run domain:build && vitest run",
    "build": "npm run domain:build && tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "functions:build": "npm run domain:build && npm --workspace rpa-license-functions run build",
    "functions:test": "npm run domain:build && npm --workspace rpa-license-functions run test",
    "validate": "npm run typecheck && npm run test && npm run build && npm run functions:build && npm run functions:test"
  },
  "dependencies": {
    "@rpa-license/domain": "file:packages/domain",
    "firebase": "^12.12.1",
    "lucide-react": "^1.14.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}

