{
  "name": "rpa-license-functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "serve": "npm run build && firebase emulators:start --only functions,firestore"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@rpa-license/domain": "file:../packages/domain",
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}

